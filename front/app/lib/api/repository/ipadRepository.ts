import axios from 'axios';
import { Ipad } from '../Entity/Ipad';
import { IpadResponseObject } from '../Factory/IpadFactory';

export class IpadRepository {
  static async get(id: number): Promise<Ipad> {
    
  }
}
